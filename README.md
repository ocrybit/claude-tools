# Claude Tools

Single-file HTML tools designed for Claude.ai's artifact preview panel. Upload the file to a chat and use it directly in the preview.

## [Draw](./draw)

A minimal diagram editor. Create rectangles, ellipses, lines, arrows, and text boxes on a snap-to-grid canvas. Style shapes with custom colors and stroke widths, arrange layers, and export as PNG or JSON.
